# Hyprlock Configuration - Blue Anime Theme
# Matching your wallpaper and wofi theme

# General settings
general {
    disable_loading_bar = false
    grace = 2
    hide_cursor = true
    no_fade_in = false
    no_fade_out = false
    ignore_empty_input = false
}

# Background with blur effect
background {
    monitor =
    path = ~/.config/hypr/wal.png   # Your current wallpaper
    blur_passes = 3
    blur_size = 8
    noise = 0.0117
    contrast = 0.8916
    brightness = 0.5
    vibrancy = 0.1696
    vibrancy_darkness = 0.0
}

# Input field for password
input-field {
    monitor =
    size = 350, 50
    outline_thickness = 3
    dots_size = 0.26
    dots_spacing = 0.15
    dots_center = true
    dots_rounding = -1
    outer_color = rgba(100, 181, 246, 0.8)
    inner_color = rgba(13, 27, 42, 0.8)
    font_color = rgba(227, 242, 253, 1.0)
    fade_on_empty = true
    fade_timeout = 1000
    placeholder_text = <i>Enter Password...</i>
    hide_input = false
    rounding = 16
    check_color = rgba(100, 181, 246, 1.0)
    fail_color = rgba(244, 67, 54, 1.0)
    fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i>
    fail_transition = 300
    capslock_color = rgba(255, 193, 7, 1.0)
    numlock_color = -1
    bothlock_color = -1
    invert_numlock = false
    swap_font_color = false
    
    position = 0, -20
    halign = center
    valign = center
}

# Main clock
label {
    monitor =
    text = cmd[update:1000] echo "$TIME"
    color = rgba(227, 242, 253, 1.0)
    font_size = 90
    font_family = SF Pro Display
    position = 0, 160
    halign = center
    valign = center
}

# Date label
label {
    monitor =
    text = cmd[update:1000] echo "$(date '+%A, %B %d')"
    color = rgba(179, 229, 252, 0.8)
    font_size = 24
    font_family = SF Pro Display
    position = 0, 60
    halign = center
    valign = center
}

# User greeting
label {
    monitor =
    text = Hi there, $USER
    color = rgba(100, 181, 246, 1.0)
    font_size = 28
    font_family = SF Pro Display
    position = 0, -120
    halign = center
    valign = center
}

# Battery status (if laptop)
label {
    monitor =
    text = cmd[update:30000] echo "🔋 $(command cat /sys/class/power_supply/BAT*/capacity 2>/dev/null || echo 'N/A')%"
    color = rgba(179, 229, 252, 0.7)
    font_size = 16
    font_family = SF Pro Display
    position = 30, 30
    halign = left
    valign = top
}

# Network status
label {
    monitor =
    text = cmd[update:5000] echo "📡 $(nmcli -t -f active,ssid dev wifi | grep -E '^yes:' | cut -d: -f2 || echo 'Disconnected')"
    color = rgba(179, 229, 252, 0.7)
    font_size = 16
    font_family = SF Pro Display
    position = 30, 60
    halign = left
    valign = top
}

# Weather (optional - requires curl and weather API)
label {
    monitor =
    text = cmd[update:300000] curl -s "https://wttr.in/?format=1" | head -1 || echo "🌤️ Weather unavailable"
    color = rgba(179, 229, 252, 0.7)
    font_size = 16
    font_family = SF Pro Display
    position = 30, 90
    halign = left
    valign = top
}

# Audio volume
label {
    monitor =
    text = cmd[update:1000] echo "🔊 $(wpctl get-volume @DEFAULT_AUDIO_SINK@ | awk '{print int($2*100)}')%"
    color = rgba(179, 229, 252, 0.7)
    font_size = 16
    font_family = SF Pro Display
    position = -30, 30
    halign = right
    valign = top
}

# System uptime
label {
    monitor =
    text = cmd[update:60000] echo "⏱️ $(uptime -p | sed 's/up //')"
    color = rgba(179, 229, 252, 0.7)
    font_size = 16
    font_family = SF Pro Display
    position = -30, 60
    halign = right
    valign = top
}

# Lock icon/decoration
label {
    monitor =
    text = 🔒
    color = rgba(100, 181, 246, 0.8)
    font_size = 48
    font_family = SF Pro Display
    position = 0, -200
    halign = center
    valign = center
}

# Keyboard layout indicator
label {
    monitor =
    text = cmd[update:1000] hyprctl devices | grep -A 1 "Keyboard at" | grep "keymap" | awk '{print toupper($2)}' | head -1 || echo "US"
    color = rgba(179, 229, 252, 0.6)
    font_size = 14
    font_family = SF Pro Display
    position = 0, -280
    halign = center
    valign = center
}
